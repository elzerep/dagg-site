# P4R6-A0-Claude — strategic/editorial first cut

Status: ready after 22:20 Europe/Stockholm usage reset  
Owner: Claude Code  
Isolation: first cut; do not inspect any Codex A/B/C route

## Read before editing

Read `CLAUDE.md`, the active P4R6 package, its acceptance matrix, the masterplan,
the Christian addendum, all three approved design PDFs, the P4R2 page map,
P4R5 component source, the P4R5B Component Intelligence Revision, the
image-production protocol, `ASSET-REGISTRY.json` and the
frozen A slot manifest. Name these authorities in the final report.

## Owned files

Only:

- `preview/golden-standard/directions/a0-claude/src/body.html`
- `preview/golden-standard/directions/a0-claude/src/direction.css`
- `preview/golden-standard/directions/a0-claude/src/direction.js`
- `preview/golden-standard/directions/a0-claude/src/meta.json`

Do not edit shared files, manifests, registry, evidence, generated `index.html`,
any Codex route, or B/C. Do not run the shared assembler.

## Direction

Reader conclusion: Dagg understands where the company must go and can make the
decision executable.

Use verbatim:

- H1: `Build the company AI makes possible.`
- Lead: `Dagg turns strategic direction into an AI-native operating model - then builds the context, agents and software that make it work.`

Copy: 390-500 visible words. Hero <=30 words beyond actions. First two
viewports <=115 words. Register: 70% strategic conclusion, 30% proof.

Use only the selected A desktop/mobile hero IDs from the frozen manifest in a
real `<picture>`. Decision Field is the world; Machine Signal is finite. No act
uses all three image languages.

## Required nine acts

Create these ordered `data-act` values, all meaningful with JavaScript blocked:

1. `hero`
2. `stakes`
3. `transformation`
4. `workgraph`
5. `factory`
6. `surfaces`
7. `operate-trust`
8. `impact`
9. `assessment`

Required rhythm: 7/5 editorial opening, short stakes punctuation, decision
continuum before product detail, concise WorkGraph/Factory proof, one contained
two-surface act, restrained assessment conclusion.

Reuse the accepted component grammar. Progressive enhancement must cover five
strategic decisions, WorkGraph states, finite Factory progression, both
operating surfaces and governed exception. Reduced motion reaches the same
final state without ambient looping.

## Reject

No header/footer copy, cards-as-page, terminal, dashboard, circuit aesthetic,
full-screen decorative image, inline SVG, CSS/div art, symbol icons, remote
asset, peer name, invented client/outcome, current managed-operations claim or
unsupported multiplier.

## Exit report

Failures first. Report exact owned files, visible word count, nine-act order,
selected asset IDs, progressive-enhancement states, mobile/reduced-motion
provisions and every unverified item. Do not claim rendering or browser QA.
