# P4R6-C0-Claude — operational/enterprise first cut

Status: ready after 22:20 Europe/Stockholm usage reset  
Owner: Claude Code  
Isolation: first cut; do not inspect any Codex A/B/C route

## Read before editing

Read `CLAUDE.md`, the active P4R6 package, its acceptance matrix, the masterplan,
the Christian addendum, all three approved design PDFs, the P4R2 page map,
P4R5 component source, the P4R5B Component Intelligence Revision, the
image-production protocol, `ASSET-REGISTRY.json` and the
frozen C slot manifest. Name these authorities in the final report.

## Owned files

Only:

- `preview/golden-standard/directions/c0-claude/src/body.html`
- `preview/golden-standard/directions/c0-claude/src/direction.css`
- `preview/golden-standard/directions/c0-claude/src/direction.js`
- `preview/golden-standard/directions/c0-claude/src/meta.json`

Do not edit shared files, manifests, registry, evidence, generated `index.html`,
any Codex route, or A/B. Do not run the shared assembler.

## Direction

Reader conclusion: Dagg can change a real enterprise operating model without
losing context, control or accountability.

Use verbatim:

- H1: `Change the operating model. Keep control.`
- Lead: `Dagg maps the real company, builds the justified agents and software, and keeps context, permissions, owners and evidence attached as work moves into execution.`

Copy: 360-470 visible words. Hero <=32 words beyond actions. Record values <=10
words. Register: 45% enterprise consequence, 35% control/proof, 20% ambition.

Use only selected C candidate 03 desktop/mobile IDs from the frozen manifest in
a real `<picture>`. The hero is Decision Field plus one restrained Machine
Signal; Operational Evidence remains contained below the opening.

## Required nine acts

Create these ordered `data-act` values, all meaningful with JavaScript blocked:

1. `hero`
2. `stakes`
3. `transformation`
4. `workgraph`
5. `factory`
6. `surfaces`
7. `operate-trust`
8. `impact`
9. `assessment`

Opening state keeps Intent, Boundary, Accountable owner and Way back visible.
Show exception before broad capability. WorkGraph carries provenance. Factory
shows where execution stops and human judgment resumes. Both surfaces preserve
the same control. Impact closes with a named truth boundary, never a fake case.

Use the accepted component grammar with static no-JavaScript reading order.
Finite enhancement must cover five decisions, WorkGraph inputs, Factory stop,
both surfaces and governed exception. Reduced motion reaches the same final
state without ambient looping.

## Reject

No header/footer copy, military aesthetic, mission-control dashboard, terminal,
circuit pattern, dense card grid, inline SVG, CSS/div art, symbol icons, remote
asset, peer name, invented client/outcome, current managed-operations claim or
unsupported multiplier.

## Exit report

Failures first. Report exact owned files, visible word count, nine-act order,
selected asset IDs, accountability record, mobile/reduced-motion provisions and
every unverified item. Do not claim rendering or browser QA.
