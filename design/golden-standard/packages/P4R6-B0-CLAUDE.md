# P4R6-B0-Claude — product/mechanism first cut

Status: ready after 22:20 Europe/Stockholm usage reset  
Owner: Claude Code  
Isolation: first cut; do not inspect any Codex A/B/C route

## Read before editing

Read `CLAUDE.md`, the active P4R6 package, its acceptance matrix, the masterplan,
the Christian addendum, all three approved design PDFs, the P4R2 page map,
P4R5 component source, the P4R5B Component Intelligence Revision, the
image-production protocol, `ASSET-REGISTRY.json` and the
frozen B slot manifest. Name these authorities in the final report.

## Owned files

Only:

- `preview/golden-standard/directions/b0-claude/src/body.html`
- `preview/golden-standard/directions/b0-claude/src/direction.css`
- `preview/golden-standard/directions/b0-claude/src/direction.js`
- `preview/golden-standard/directions/b0-claude/src/meta.json`

Do not edit shared files, manifests, registry, evidence, generated `index.html`,
any Codex route, or A/C. Do not run the shared assembler.

## Direction

Reader conclusion: Dagg has built a concrete transformation mechanism.

Use verbatim:

- H1: `Turn company context into working systems.`
- Lead: `Dagg decides what should change, captures the operating logic in WorkGraph, and builds the agents or software that put it to work.`

Copy: 270-380 visible words. Hero <=25 words beyond actions. Every state value
<=12 words. Register: 35% strategic framing, 65% inspectable mechanism.

Use only selected B candidate 04 desktop/mobile IDs from the frozen manifest in
a real `<picture>`. The hero is Decision Field plus one restrained Machine
Signal; Operational Evidence remains contained below the opening.

## Required nine acts

Create these ordered `data-act` values, all meaningful with JavaScript blocked:

1. `hero`
2. `stakes`
3. `transformation`
4. `workgraph`
5. `factory`
6. `surfaces`
7. `operate-trust`
8. `impact`
9. `assessment`

One persistent company-specific record must change across Context, Decision,
Build, Permission, Action and Evidence. The decision continuum changes that
record's build plan; Factory is the dominant proof; both surfaces preserve the
same context, permission and evidence.

Use the accepted component grammar with static no-JavaScript reading order.
Finite enhancement must cover five decisions, WorkGraph inputs, Factory state,
two surfaces and governed exception. Reduced motion reaches the same final
state without ambient looping.

## Reject

No header/footer copy, six unrelated cards, terminal, dashboard, chat clone,
circuit aesthetic, particle wallpaper, inline SVG, CSS/div art, symbol icons,
remote asset, peer name, invented client/outcome, current managed-operations
claim or unsupported multiplier.

## Exit report

Failures first. Report exact owned files, visible word count, nine-act order,
selected asset IDs, persistent-object states, mobile/reduced-motion provisions
and every unverified item. Do not claim rendering or browser QA.
