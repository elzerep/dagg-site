# Dagg golden-standard website — completion matrix

Status: active goal audit  
Date: 3 September 2026  
Completion claim: **not achieved**  
Publication status: **not authorized**  
Controlling implementation authority:
`FABLE-5.1-WHOLE-SITE-GOLDEN-STANDARD-PLAN.md`, the reconciled narrative
authorities and the FS5 writer packages.

This matrix prevents a partial visual or machine-test pass from being reported
as completion of the full Dagg website objective. `Achieved` means the exact
required scope has direct, current evidence. `Partial` and `missing` are not
passes.

All implementation, responsive, interaction, accessibility, motion and console
evidence must come from one immutable browser snapshot ID and content hash.
Evidence from different revisions may never be combined into a pass.

## 1. Narrative and positioning

| Requirement | Authority and proof | State | What remains |
|---|---|---|---|
| Dagg is positioned as strategic judgment plus real execution, not consultancy copy or generic AI software | `narrative/HOME-NARRATIVE-FOUNDER-RESET.md`; six-act argument in P4R10 | Partial — authoritative copy and desktop gate only | Verify the selected browser implementation and supporting routes preserve the same position. |
| Home is the complete but uncrowded company story | Founder-reset acts 1–6; 430–500 word contract | Partial — copy authority and desktop designs only | Verify exact public-field presence and rendered comprehension on desktop, tablet and mobile. |
| WorkGraph and Dagg Factory are central, named capabilities inside the company story | Founder-reset acts 3–5 | Partial — Home gate only | Verify implementation and supporting-route depth without duplicate or conflicting copy. |
| Internal AI-native transformation leads; the external customer surface is one governed consequence | Founder-reset act 4, P4R4 addendum and Build map | Partial — FS5 exact copy only | FS5 must preserve the copy and boundary without a fake interface. The stateful two-surface proof is explicitly assigned to FS6 using the P4R5 contract. |
| Urgency is felt without threat or unsupported hype | Founder-reset rejection criteria | Partial — source copy only | Requires exact rendered judgment on the immutable browser candidate. |
| Full supporting-route narrative is aligned with the current Home | `narrative/FULL-SITE-CONTENT-CONTRACT.md` and the 205-unit exact-copy manifest control shared chrome and every FS5 route | Partial — authority reconciled | Verify Transformation, WorkGraph, Build, Company and Assessment in the immutable browser candidate. |

## 2. Visual system and art direction

| Requirement | Authority and proof | State | What remains |
|---|---|---|---|
| Anthropic-inspired abstract image system with a distinct Dagg grammar | `authority/Dagg-Image-Language-System.pdf`; Decision Field, Operational Evidence and Machine Signal | Achieved as a system | Verify every selected production asset, crop and viewport composition. |
| Warm, premium foundations with strategic restraint and machine precision | `authority/Dagg-Design-System-Foundations.pdf` | Achieved as foundations | Verify production tokens and actual rendered contrast/type/spacing. |
| Component and interaction rules match the visual system | Component authority PDF, P4R5B and P4R10 §5 | Partial | Shared rules are frozen; selected-direction component integration and browser evidence do not yet exist. |
| A/B/C were genuinely different worldviews and produced a selected Home direction | Figma nodes A `15:2`, B `25:2`, C `33:2`; founder feedback; protected FS5 Home invariant baseline | Achieved as direction selection | The selected Home is frozen; complete iPad/mobile implementation still requires same-snapshot browser evidence. |
| Exact reviewed visuals are immutable | Seven PNG exports plus SHA-256 manifest | Achieved for the desktop founder gate | A new manifest revision is required after any Figma change. |
| Independent visual review has no blocking authority defect | Codex narrative and visual audits; `authority/FS5-VISUAL-PAIR-REVIEW-PROTOCOL.md` | Achieved for authority, not implementation | Fable activation and the post-build paired visual review remain pending. |
| Production image allocation follows the binding system on every route and viewport | Home is Decision Field primary; Machine Signal restrained and finite; Operational Evidence contained to inspectable proof; maximum two roles per section; no terminal/dashboard/node-map/biological forms; separately authored tablet/mobile compositions | Missing | Verify every production section, source asset, crop, state and viewport against the image-language authority. |

## 3. Direction and collaboration gates

| Requirement | Authority and proof | State | What remains |
|---|---|---|---|
| Christian selects the Home worldview | Founder feedback and `FS5-PROTECTED-INVARIANT-BASELINE.json` | Achieved | Hero copy, assets and lossless rendered references are protected. |
| Fable 5.1 independently reconciles the exact FS5 authority | `packages/FS5-FABLE-5.1-ACTIVATION-REVIEW.md` | Original activation achieved; narrow post-activation checksum review pending | The original review returned `ACTIVATED`. Shared-chrome implementation then exposed four existing responsive preview labels without keyed ownership. Exact authority now has 205 unique units, 16 accessible units, seven explicit exclusions and a 37-test pure QA pass. Revalidate the two changed hashes before Lane A/B dispatch. |
| One writer owns each source file | Active FS5 Lane A/B/C packages; P4R7/P4R8/P4R8R1/P4R11 marked historical | Achieved as governance | Enforce the active disjoint ownership through FS5 assembly. |
| Stale five-beat Home cannot regain authority | Masterplan Home exception; superseded banners; P4R10 authority order | Achieved | Re-audit after any new package is created. |

## 4. Implementation

| Requirement | Authoritative evidence required | State | What remains |
|---|---|---|---|
| Selected Home is implemented from the protected direction and six-act copy | `FS5-LANE-A-CLAUDE-WRITER.md` plus protected invariant baseline | Missing | Existing `preview/golden-standard/home/` remains the rejected input candidate. Lane A starts only after the narrow refreshed Fable checksum verdict. |
| Real Dagg logo, routes and CTAs are used | Frozen source plus link audit | Missing | Implement and verify. |
| Assessment conversion works end to end | Same-snapshot browser and backend-truth evidence for initial, validation, submitting, success, error and duplicate states; consent/privacy copy; keyboard completion; resilient no-JS path or explicit truthful dependency | Missing | Link existence is not conversion evidence. Verify form payload handling without sending real production enquiries during QA. |
| Adaptive Build navigation works | Pointer, keyboard, touch, hybrid and no-JS traces from one immutable build | Missing | FS5 uses a native `<details><summary>` named `Build`, followed by `/build`, `/build#factory`, `/build#factory-modes` and `/trust`. The split overview-link plus adjacent disclosure button is an FS6 migration target. |
| Selected signature behavior works in normal motion | Time-based capture and state assertions from the immutable selected build at representative desktop, iPad and mobile sizes, including trigger, progression, end state and pause/replay where relevant; interrupted motion never removes meaning | Missing | Verify the selected Home execution instrument, Transformation continuum, WorkGraph lifecycle and Build Factory pass. Each sequence is finite, preserves complete core meaning and resolves without a click dependency. |
| Supporting routes form one connected book | Complete route inventory, builds and link graph from the same immutable snapshot | Partial | Verify Transformation, WorkGraph, Build including Factory/Agents/Trust and operation, Company, Assessment, Trust, Privacy, Terms and 404. Impact must either have approved provenance and a complete public route or be atomically absent from nav, metadata, sitemap and public build. |
| No unsupported deployment, operations or outcome proof appears | Rendered copy audit against evidence ledger | Missing | Run on the selected immutable build. |
| The complete P4R10 executable gate passes atomically | P4R10 §11.1–15 all PASS on the same immutable snapshot ID and content hash | Missing | No subset or mixed-revision evidence may satisfy this row. |

## 5. Responsive, interaction and accessibility verification

| Requirement | Required evidence | State |
|---|---|---|
| Desktop at 1440×900, 1366×768 and 1280×720 | Rendered implementation from the same immutable snapshot; frozen screenshots, DOM metrics, semantic reading-order and visible review | Missing |
| Tablet at 1024×768, 834×1194 and 768×1024 | Rendered implementation from the same immutable snapshot; authored composition, same semantic DOM, frozen screenshots and visible review | Missing |
| Mobile at 390×844, 360×800 and 320×800 | Rendered implementation from the same immutable snapshot; authored composition, same semantic DOM, frozen screenshots and visible review | Missing |
| No horizontal overflow, clipped essential copy or missing default conclusion | `scrollWidth === clientWidth`, essential-copy presence and screenshot evidence at every required viewport on the same snapshot | Missing |
| Adaptive Build semantics and every input mode pass | FS5: native summary named `Build`, first child real `/build` overview link, then `/build#factory`, `/build#factory-modes` and `/trust`; matching preview states; stable outer geometry; Enter/Space/Escape; Tab order; focus restoration; mobile focus containment and background `inert`; deterministic fine/coarse/hybrid routing. FS6 production parity may then split the overview link and adjacent disclosure control. | Missing |
| Pointer, keyboard, touch and hybrid-input equivalence | Complete interaction traces and assertions for all required capability modes on the same snapshot | Missing |
| Reduced motion implements the selected direction's full semantic fallback | A: resolved composition and complete copy immediately. B: all four complete states in document order, no stickiness/timers/scroll-linked transforms. C: frame 04 conclusion plus causal labels immediately. | Missing |
| No-JavaScript preserves six acts and the complete Build fallback | No-JS screenshot, DOM and link audit proving `/build`, `/build#factory`, `/build#factory-modes` and `/trust`; static fallback present; no inert enhancement controls | Missing |
| Type, zoom, contrast, semantics and target-size floors pass | Minimum 12 px public type; 200% zoom/reflow; 44×44 desktop and 48×48 touch targets; accessible names; disclosure semantics; visible focus; background `inert`; computed evidence and visible review | Missing |
| Zero console errors in the complete state inventory | Logs from all viewports, four Build preview states, flyout/sheet/hybrid disclosure modes, all selected signature states, reduced motion, no-JS and capability modes on the same snapshot ID | Missing |

## 6. Performance and delivery verification

| Requirement | Required evidence | State |
|---|---|---|
| Mobile Core Web Vitals meet the production gate | Field-equivalent or controlled repeatable run on the exact immutable candidate: LCP <2.5 s, INP <200 ms and CLS <0.1 | Missing |
| Asset and transfer budgets are explicit and pass | Per-route transfer inventory, image/video/font/JS/CSS weights and largest-request audit on the same candidate | Missing |
| Images and video load without breaking the visual or reading path | Correct intrinsic dimensions, responsive sources, modern formats, lazy/eager priority by narrative role, poster/fallback states and verified failure behavior | Missing |
| Interaction remains responsive under motion and media load | Main-thread and interaction trace for navigation, signature behavior and Assessment path | Missing |

## 7. Visible-quality and peer-standard verification

| Requirement | Required evidence | State |
|---|---|---|
| Selected site feels at least as considered as Anthropic, OpenAI, Palantir and xAI while remaining Dagg | Same-viewport comparison boards at representative desktop, iPad and mobile sizes using current official peer captures and the exact selected Home plus principal Transformation, WorkGraph and Build route patterns | Missing |
| Design feels premium, calm, intelligent and not template-like | Codex and Claude failure-first review of identical frozen evidence | Missing |
| Story reads within and between routes | Headline-spine and full-copy read-through on the rendered site | Missing |
| Images, motion and product proof perform the local narrative job | Exact asset/crop/state inspection at every route and viewport | Missing |

## 8. Metadata, indexation and publication gate

| Requirement | State | Rule |
|---|---|---|
| Every public route has correct title, description, canonical and social preview | Missing | Verify rendered head and image URLs per route from the immutable candidate. |
| Sitemap, robots and structured data match the approved public route inventory | Missing | Impact and any unapproved proof route must be absent; no stale preview route may be indexable. |
| Trust, Privacy, Terms and 404 are reachable and accurate | Missing | Legal/trust routes are part of publication preflight, not optional polish. |
| Exact production candidate has an immutable revision ID and complete evidence bundle | Missing | No mutable or mixed-revision preview may be approved. |
| Codex and Claude both pass the exact production candidate | Missing | Neither may report the other's judgment. |
| Christian explicitly approves publication of that exact revision | Missing | No approval is inferred from choosing A, B or C. |
| Site is published | Intentionally not started | Publication is forbidden until all prior gates pass and Christian explicitly authorizes it. |

## 9. Immediate next actions

1. Fable 5.1's original activation is recorded in
   `packages/FS5-FABLE-5.1-ACTIVATION-REVIEW.md`; Lane A and Lane B remain
   paused until Fable confirms the refreshed contract and manifest checksums
   after the narrow four-label ownership amendment.
2. Codex completes the shared-chrome slice first without touching either Claude
   lane.
3. Obtain the exact refreshed Fable activation verdict, then dispatch mutually
   exclusive Claude Lane A and Lane B in parallel. After both report, Codex
   completes Impact and Assessment in Lane C.
4. Assemble exactly once; freeze one immutable snapshot; run the complete FS5 browser
   and evidence package.
5. Run caption-free paired visual review and final Fable review on that exact
   snapshot; repair and repeat until both pass.
6. Present the founder-reviewable candidate to Christian. Publication remains
   a separate explicit launch decision.

Route-source writing is active only through the assigned FS5 lane packages.
Publication remains forbidden.
