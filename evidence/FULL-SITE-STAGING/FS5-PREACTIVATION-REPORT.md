# FS5 pre-activation evidence

Date: 3 September 2026  
State: original Fable activation complete; doc-only scope reconciliation
incorporated; narrow four-label copy-ownership amendment awaits refreshed
Fable checksum review before writer dispatch  
Publication: not authorized and not attempted

## Verdict

The first exact Fable 5.1 activation review returned HOLD with six P1
contradictions. Each received the smallest authority or rig repair. The exact
activation session `10a146dc-d520-46dc-a183-32029bca9d8c` then returned
`ACTIVATED — FS5 may proceed through the two bounded Claude writer lanes.` A
subsequent scope-reconciliation session
`6bb69738-1cf5-4350-b15a-9a8e381363ab` returned `DISPATCH AFTER DOC-ONLY
CORRECTIONS`; those orchestration and scope corrections are now incorporated.
During the shared-chrome implementation Codex found that four existing
JS-created responsive Build-preview controls had public labels without keyed
manifest ownership. The exact contract now names those four labels and the
manifest now owns them. This did not change visible design or interaction, but
it changed two activation hashes, so the Fable writer lanes remain paused until
a refreshed read-only verdict confirms the amendment.

This is not visual acceptance or a release verdict. The currently assembled
site is the rejected input candidate and has not been promoted.

## Authority checks

- Independent narrative authority review: PASS with no remaining P0/P1
  contradiction.
- Independent visual authority review: PASS with no remaining P0/P1
  contradiction.
- Exact copy manifest: 205 unique units, 16 accessible-name or alt units, seven
  explicit zero-multiplicity FS5 exclusions, zero blockers.
- The adaptive Build disclosure now has eight sibling copy units: four exact
  destinations and four exact proof states. Privacy and Terms are explicit
  FS5 exclusions and remain mandatory FS6 publication dependencies.
- All copy-manifest authority hashes match the current files.
- Home execution uses four independent exact-text state units; only the
  execution status is dynamic. This permits all four state bodies to remain
  simultaneously present in true no-JavaScript output.
- Writer ownership is disjoint across Claude lane A, Claude lane B and Codex
  lane C. None may assemble or publish.

## Rig checks

Command:

`PYTHONDONTWRITEBYTECODE=1 python3 -B -m unittest tests.test_fs5_candidate -v`

Result:

- 37 pure contract tests passed.
- 10 completed-package tests skipped as designed because no promoted FS5
  evidence directory exists before implementation and assembly.
- Python source compilation passed without writing bytecode.
- No unconditional `KNOWN_UNIMPLEMENTED_GATES` remain.

Current post-fix falsification suite: PASS. It ran all 47 tests
(37 passed, 10 package-only skips) and separately confirmed that unknown
requested states, visible inactive-state leakage, hidden active-state copy and
accessibility-tree fallback all fail closed. All eight activation-review file
hashes matched the package at the time of the audit.

The rig encodes exact keyed copy, explicit applicable-copy states, true
no-JavaScript DOM reconstruction, browser accessibility-tree names,
accessible-only ancestor handling without swallowing visible copy, authority
hashes, explicit rejection of unknown or visibly leaked inactive states and
hidden active-state copy, protected Home assets and rendered baselines,
one-snapshot identity,
72 route/viewport matrix rows, opened-state axe checks, real CDP touch
traversal, pointer and keyboard interaction, interval-level Home, Factory and
flyout timing, reduced motion, Assessment truthfulness, atomic evidence
promotion and external visual-pair judgment.

## Protected Home baseline

Authority:
`evidence/FULL-SITE-STAGING/FS5-PROTECTED-INVARIANT-BASELINE.json`

Lossless rendered references:

- 390×844: `d89675d7dd3c8881136b8677fb764acf2b8ecda8ecbed5e796a9c8e2c1c8b44c`
- 1024×768: `206896c60b62d4fa7ba99d0e8c7397b6eb0ba2f8d72cb04d0ab39ebdc91cac68`
- 1440×900: `8f3532df14eaadf2178b58398117cd5967591fa0b518907b9ecb43645c013962`
- 1536×1024: `a6959a206d683c9b1817a100323232608624e76e0b9670d6e93409567cb35713`

## Required next state

1. Codex completes the shared-chrome slice.
2. Fable confirms the refreshed contract and manifest checksums after the
   narrow four-label ownership amendment and returns the exact activation
   verdict recorded by the activation package.
3. The two non-overlapping Fable writer lanes complete and report; Codex then
   completes Assessment and Impact within Lane C.
4. Codex assembles exactly once and freezes one immutable candidate.
5. Full browser evidence and the external paired visual review pass against
   that same snapshot.
6. Final Fable review returns no P0/P1.
7. Christian reviews the founder-ready candidate. Publication remains a
   separate explicit decision.
