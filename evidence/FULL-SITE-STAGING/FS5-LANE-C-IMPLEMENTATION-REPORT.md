# FS5 Lane C implementation report — Assessment, Impact and shared chrome

Date: 3 September 2026  
State: source complete; ready for the single controlled assembly  
Publication: not authorised and not attempted

## 1. Failures and holds first

1. `node tools/build_golden_standard_previews.mjs --check` reports only the
   expected `outputs-stale` failure. Source changed while assembly remained
   intentionally closed. All six structural assembly checks pass.
2. Assessment's first section contains 47 words against the 30–45 soft target.
   The complete default path is 385 words and the progressive path is 180,
   both inside their binding bands. Exact contract copy was not silently
   trimmed to optimise a two-word soft overage. The rendered page will decide
   whether a controlled copy amendment is justified.
3. Rendered quality remains unproven before assembly: geometry, H1 line count,
   touch targets, menu behaviour, form focus, reduced motion, true no-JavaScript,
   accessibility and visual comparison still require the immutable browser run.
4. Impact remains an internal, noindex staging route and is absent from shared
   navigation. No case, client, metric, deployment or logo has been invented.
5. The controller amended the Company R5 viewport contract and WorkGraph route
   root after the Lane A and B writer reports. The final source hashes and
   superseding decisions are sealed separately in
   `FS5-POST-LANE-SOURCE-FREEZE.md`; the stale writer-report deviations are not
   current authority.

No other deviation from the Lane C package is known.

## 2. Exact changed files and reasons

| File | Change |
|---|---|
| `preview/golden-standard/routes/assessment/src/body.html` | Exact keyed copy; six-section narrative; one consequential-path scope; local inquiry form; truthful no-JavaScript and local-preview success states. |
| `preview/golden-standard/routes/assessment/src/direction.css` | Responsive composition, balanced H1 measure, 48 px touch target, form states and reduced-motion behaviour without new tokens or reordering. |
| `preview/golden-standard/routes/assessment/src/direction.js` | Local-only reveal, validation, focus and 450 ms preview state; no request is sent. |
| `preview/golden-standard/routes/impact/src/body.html` | Exact internal proof architecture and empty-state disclosure; no unsupported Aloi or client claim. |
| `preview/golden-standard/routes/impact/src/direction.css` | Route-specific composition using the existing system only. |
| `preview/golden-standard/routes/impact/src/direction.js` | Existing inert route script retained; no unsupported behaviour added. |
| `preview/golden-standard/shared/chrome/header.html` | Exact shared copy ownership, routes, adaptive Build disclosure and decorative theme-swapped marks. |
| `preview/golden-standard/shared/chrome/footer.html` | Exact footer ownership and gated Impact state; Privacy and Terms remain FS6 dependencies. |
| `preview/golden-standard/shared/chrome/chrome.js` | Atomic open/closed menu label state while preserving pointer, keyboard, touch, Escape, focus restore, inertness and scroll lock. |
| `preview/golden-standard/component-library/script.js` | Exact ownership on the four existing responsive Build-preview controls only. |
| `evidence/FULL-SITE-STAGING/FS5-LANE-C-IMPLEMENTATION-REPORT.md` | This final report. |

`chrome.css` required no Lane C change.

## 3. Copy, claims and route boundaries

- Assessment: 385 default-visible words; 180 progressive words; 47 words in
  the first section.
- Exact validation strings: `Enter your name.`, `Enter a valid work email.`,
  `Enter your company.`, `Describe one workflow or operating path.`, and
  `Explain why the decision matters now.`
- Exact no-JavaScript path: `The inquiry form requires JavaScript and does not
  transmit from this preview. Email hello@dagg.ai to begin.`
- Exact local success truth: `The inquiry is ready for review.` followed by
  disclosure that no data was sent and production handling remains required.
- Impact status: `Approved proof · none published`.
- Impact disclosure says publication requires an approved company, role,
  evidence, timeframe and permission. Impact is absent from header and footer.
- No visible copy, accessible name or generated state falls outside
  `COPY-UNIT-MANIFEST.json`.

## 4. Shared navigation and accessibility contract

- Public destinations: Home; Transformation; WorkGraph; Build overview; Dagg
  Factory; Agents and software; Trust and operation; Company; Start an
  assessment.
- Impact is not a public destination.
- The Build control remains a native disclosure named `Build`; its first child
  is the real Build overview route.
- Native disclosure semantics carry expanded/collapsed state. No ARIA `menu`
  or `menubar` role was invented.
- Both theme-swapped header images are decorative inside the exactly labelled
  Home link. The footer logo alone owns the `Dagg` alt text.
- Assessment reveals its form from the primary action, focuses the first invalid
  field, guards duplicate local timers, and never makes an outbound request.
- DOM and reading order are unchanged at every width; CSS `order` is absent.

## 5. Source-level verification

- JavaScript syntax: pass for Assessment, Impact, shared chrome and the shared
  component script.
- Strict copy reconciliation: pass; zero missing, extra, nested, mismatched or
  uncovered visible/accessibility units.
- Pure FS5 contract suite: 47 tests run; 37 pass; ten browser-evidence tests
  skip as designed before the evidence package; zero failures.
- Assembly source check: six structural checks pass; only the expected stale
  generated outputs remain.
- Nothing was assembled, committed, pushed, deployed or published in this lane.

## 6. Final authority

- Full-site content contract:
  `c7068104a28fa6a2e6f104daecbdec8410ec5c719fcf00f55f47432ed03a07dc`
- Fable whole-site plan:
  `84a8014ed3fbc1e4e9744932024bf9c08f5ebff6533f81fef945e533ab0b62bb`
- Copy-unit manifest:
  `a7d8b0baabdda5a1807a74f1ea3c73c50ad5342bdd39bcee4bfaddec5a80d801`

## 7. Required continuation

1. Seal all final lane source in the post-lane freeze report.
2. Assemble exactly once.
3. Freeze and run the complete Chrome evidence package across desktop, tablet,
   mobile, reduced motion and no-JavaScript states.
4. Fix only evidence-backed failures, reassemble and rerun when necessary.
5. Obtain the final independent Fable 5.1 acceptance before presenting the
   local candidate. Do not publish without Christian's explicit approval.
