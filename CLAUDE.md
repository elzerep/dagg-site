# Dagg website execution authority

Read `design/golden-standard/DAGG-GOLDEN-STANDARD-MASTERPLAN.md` before changing this repository.

The masterplan overrides earlier design, sitemap, template, measurement and preview documents whenever they conflict. Those older files remain evidence and implementation context, not final authority.

Work one bounded package at a time. FS5 is active across the two Fable writer
lanes and Codex lane C under mutually exclusive file ownership. The current
execution authority is the following package set:

- `design/golden-standard/packages/FS5-LANE-A-CLAUDE-WRITER.md`
- `design/golden-standard/packages/FS5-LANE-B-CLAUDE-WRITER.md`
- `design/golden-standard/packages/FS5-LANE-C-CODEX-IMPLEMENTATION.md`

The activation decision is recorded in
`design/golden-standard/packages/FS5-FABLE-5.1-ACTIVATION-REVIEW.md`. Fable 5.1
returned the exact verdict `ACTIVATED — FS5 may proceed through the two bounded
Claude writer lanes.` The later scope-reconciliation read returned `DISPATCH
AFTER DOC-ONLY CORRECTIONS`; those corrections are incorporated in the current
packages. While implementing shared chrome, Codex found four existing
JS-created responsive Build-preview controls whose accessible labels were not
owned by the exact manifest. The contract and manifest now own those four
labels. The final refreshed Fable 5.1 pass recomputed all eight identities,
confirmed 533 Build words inside the 360-535 band, ran 47 candidate tests with
ten expected pre-assembly skips and zero failures, and returned `ACTIVATED`
with no P0/P1 blocker. Lane A and Lane B are therefore dispatched. FS4B remains
historical failure-first evidence and is not active.

The full-site narrative contract remains the working authority. After an
independent fail-first Claude rejection on 1 September 2026, Home's contract,
three maps and rendered source were reconciled. Claude then returned
`ACCEPT-WITH-CHANGES` with no narrative blocker for assembled revision
`648cddee0e34be069d917556d8bea9d2bbdfa9f789904cdb9439b51104c3493c`.
Home may now continue through the still-open responsive, accessibility, motion,
navigation and visible-quality gates. The content contract supersedes P4R7 Home
copy/story rules where it says so; P4R7 visual, responsive, interaction and QA
requirements remain binding where the accepted maps have not refined them.

P4R4 is diagnostic evidence, not an accepted Home direction. Read this binding
addendum immediately after the active package:

`design/golden-standard/packages/P4R4-CHRISTIAN-AUTHORITATIVE-ADDENDUM.md`

Before any visual implementation or visual acceptance, read both approved
source documents in full. They are the authority for the visual system and may
not be replaced by a summary:

- `design/golden-standard/authority/Dagg-Image-Language-System.pdf`
  (SHA-256 `67f3d41d85106ef9fb3947598229eb6f75f4d25ce3b0217e0a687f8123768726`)
- `design/golden-standard/authority/Dagg-Design-System-Foundations.pdf`
  (SHA-256 `cc425b44ff0a26b225a6e34317b41f4a907f423645d87ff4038a5f1f53108eca`)
- `design/golden-standard/authority/Dagg-Component-and-Interaction-Library.pdf`
  (SHA-256 `0045db6cdfaf74d1404f59e6fedd8a2cc4c203d43862d1c63d09257d271dfd9d`)

The Component and Interaction Library is governed by the binding revision:

`design/golden-standard/packages/P4R5B-COMPONENT-INTELLIGENCE-REVISION.md`

It retains the accepted 40/40 functional substrate while imposing a sparse
public-site icon budget, a two-layer utility/semantic architecture and an
adaptive-intelligence contract. The larger semantic icon collection is for
product UI, presentations and future documentation; it is not a usage quota for
the marketing site. Claude builders and judges must read this revision before
evaluating or synthesizing any P4R6 direction.

Then read `design/golden-standard/packages/P4R2-THREE-WAY-DIRECTION-GATE.md`
only as historical image-role and ratio evidence from the rejected direction
gate. It is not the current page-composition authority. The current FS5 page
maps under `design/golden-standard/design-mapping/` govern composition,
including the selected full-width Home hero band. Every Claude worker and judge
that evaluates imagery, page composition, motion or interaction must name the
three design-system sources and the applicable current FS5 page map in its
report. If a worker cannot access either PDF, it must stop and report the
missing source instead of judging from memory.

For image selection, generation requests and asset use, also read:

`design/golden-standard/image-system/IMAGE-PRODUCTION-PROTOCOL.md`

For the exact approved P4R6 image bank, narrative jobs, copy counterparts,
responsive compositions and future expansion rules, also read:

`design/golden-standard/image-system/IMAGE-LIBRARY-CATALOG.md`

Codex owns the shared image-generation lane. Claude may request a narrative
slot and compose approved assets, but may not create a parallel image grammar.

Rules:

1. P2 and P3 foundations, the P4R5 component/interactions specimen, FS2's WorkGraph/Factory carriers and FS3 Home are accepted inputs, not immunity from review. P4R3, P4R4, P4R6, FS4B and the prior A2 render are diagnostic evidence. Execute only the assigned FS5 lane package.
2. Change only files the active package owns.
3. Use only the exact copy, labels, link targets and claim boundaries defined by the active content contract. Do not improvise marketing claims.
4. Report failures and deviations before successes.
5. Machine tests do not overrule visible rendering failures.
6. Every claim of verification must name the rendered revision, viewports, states and actual coverage.
7. Codex owns routine design/implementation acceptance. Do not ask Christian for micro-decisions.
8. The system hierarchy is already selected: Decision Field surrounds and explains, Operational Evidence proves, Machine Signal may transition states. The image portfolios are generative grammars, not fixed assets. Do not ask Christian to choose again or request micro-approval.
9. Claude writers may edit only the source and report files named by their active FS5 lane. Codex alone owns shared assembly, acceptance evidence and publication-state integration.
10. Never deploy or publish without Christian's explicit approval.

Return the failure-first implementation report required by the assigned FS5
lane. Do not assemble, commit, push, deploy or publish. Do not touch any file
owned by another lane.

## Historical P4R6 execution state — 31 August 2026; never resume

- Shared assembly is accepted for route construction. Read
  `evidence/HOME-DIRECTIONS/shared-substrate/SUBSTRATE.md` and preserve the
  exact import order in its build manifest.
- A/B/C hero manifests are frozen. Use only the selected desktop/mobile IDs
  in `design/golden-standard/image-system/slot-manifests/{a,b,c}.json` and the
  matching hashes in `ASSET-REGISTRY.json`.
- Codex first cuts may already exist under `a0-codex`, `b0-codex` and
  `c0-codex`. Until the formal freeze and cross-review step, Claude builders
  must not inspect those routes.
- Claude builders own only their matching
  `preview/golden-standard/directions/{a0,b0,c0}-claude/src/` files. They must
  not run the shared assembler while another first cut is active; Codex will
  assemble all six after every isolated source route is complete.
- Run each Claude cut as a separate bounded job using its direction package:
  `P4R6-A0-CLAUDE.md`, `P4R6-B0-CLAUDE.md` or `P4R6-C0-CLAUDE.md`. Never merge
  them into one broad generation prompt.
- The earlier Claude session ended at the service usage boundary, not because
  the package failed. Preserve this only as provenance; never resume its writer
  scopes or recreate or overwrite the accepted substrate.
